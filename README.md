# PacketTracer-OSPF-MultiVLAN-Network
A Cisco Packet Tracer network project implementing OSPF routing between four routers. The topology consists of two interconnected networks, where the first network includes six VLANs and the second network includes three VLANs. The project demonstrates inter-VLAN routing, trunking, and dynamic routing using OSPF.
